<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">

        <activity
            android:name=".activities.InicioActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.InicioSesionActivity"></activity>
        <activity android:name=".activities.RegistroActivity"></activity>
        <activity android:name=".activities.MenuPrincipalActivity"
            android:exported="true">

        </activity>
        <activity android:name=".activities.PerfilActivity"></activity>
        <activity android:name=".activities.SubirAnuncioAcitivity"></activity>
        <activity android:name=".activities.CatalogoActivity"></activity>
        <activity android:name=".activities.PublicacionActivity"></activity>
        <activity android:name=".activities.ChatActivity"></activity>
        <activity android:name=".activities.FAQActivity"></activity>
        <activity android:name=".activities.BookmarksActivity"></activity>
        <activity android:name=".activities.AcercaDeActivity"></activity>










    </application>

</manifest>